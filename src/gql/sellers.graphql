import gql from 'graphql-tag'

export const ALL_SELLERS = gql`
  query allSellers {
    sellers {
      id
      firstName
      lastName
      email
      items {
        id
        title
        price
      }
    }
  }
`