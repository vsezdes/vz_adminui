import gql from 'graphql-tag'

export default gql`
  query categoryItems($id: Int!){
    categoryItems(id: $id) {
      id
      title
      price
      images {
        asset_id
        url
      }
    }
  }
`
